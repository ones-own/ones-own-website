{
  "private": true,
  "name": "website",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "build": "parcel build src/index.html",
    "start": "parcel serve src/index.html",
    "check:css": "stylelint '**/*.css' --ignore-path .gitignore",
    "check:format": "prettier '**/*.{css,html,js,md,yml}' --check --ignore-path .gitignore",
    "test": "npm-run-all check:*",
    "format": "prettier '**/*.{css,html,js,md,yml}' --write --ignore-path .gitignore"
  },
  "devDependencies": {
    "@signal-noise/stylelint-scales": "^1.5.0",
    "autoprefixer": "^9.8.6",
    "logical": "^0.1.0",
    "npm-run-all": "^4.1.5",
    "parcel": "^2.0.0-beta.1",
    "postcss-logical": "^4.0.2",
    "postcss-nesting": "^7.0.1",
    "prettier": "^2.1.2",
    "stylelint": "^13.8.0",
    "stylelint-config-recommended": "^3.0.0",
    "stylelint-order": "^4.1.0",
    "stylelint-use-logical": "^1.1.0",
    "stylelint-use-nesting": "^3.0.0"
  },
  "dependencies": {
    "dark-mode-toggle": "^0.7.3"
  }
}
